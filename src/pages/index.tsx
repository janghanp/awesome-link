import { Container } from "@mantine/core";

export default function Home() {
  return (
    <Container
      style={{
        borderStyle: "solid",
        minHeight: "100vh",
        display: "flex",
        flexDirection: "column",
      }}
    ></Container>
  );
}
